name: todo
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^5.0.0

dev_dependencies:
  build_runner: ^1.9.0
  flutter_test:
    sdk: flutter
  lint_strict_dependencies:
    path: "../../"

flutter:
  uses-material-design: true
